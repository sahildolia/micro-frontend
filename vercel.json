{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "builds": [
    {
      "src": "main-app/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "main-app/build" }
    }
  ]
}
